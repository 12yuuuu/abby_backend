{
    "configurations": [
        {
            "name": "FastAPI",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "python": "${workspaceFolder}/.venv/bin/python", // 新增关键配置
            "args": [
                "src.main:app",
                "--host",
                "0.0.0.0",
                "--reload"
            ],
            "jinja": false
        }
    ]
}